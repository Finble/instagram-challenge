<h3>Comments for <%= picture.name %></h3>

<% if picture.comments.any? %>
  <ul>
    <% picture.comments.each do |comment| %>
      <li>
        <%= comment.comments %>
    <% end %>
  </ul>
<% else %>
  <p>No comments</p>
<% end %>